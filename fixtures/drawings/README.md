# Drawing fixtures

Synthetic canonical models used only to verify deterministic derived drawing views.
They contain no customer or production data. `v1/drawing-model.json` uses the merged
canonical model v1 contract and intentionally includes two levels, building geometry,
an opening, electrical equipment/devices, a route/fittings, a circuit, and an obstacle.
