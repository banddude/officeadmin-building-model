# Quantity fixtures

`synthetic-route-model.json` is a canonical v1 `BuildingModel` with hand-authored
fake `Route` objects. It exists so the quantities lane can prove known-answer
raceway/cable lengths, fittings, conductors, devices, and equipment without
waiting for the routing implementation or measuring any drawing/PDF.
